import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nyp.skippyq',
  appName: 'skippyq',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};



export default config;
